export const PUBLIC_ROUTES = ["/", "/login", "/register"] as const;

export const AUTH_ROUTES_PREFIX = ["/login", "/register"] as const;

export const DEFAULT_LOGIN_REDIRECT = "/dashboard";

export const USER_PREFIX = "/dashboard";
